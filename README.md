# Regression_Project-on-House_Price_Prediction
A machine learning-based Property Price Prediction web application that predicts estimated property prices from user-provided features. The project uses a trained Python ML model (.pkl), Flask for the backend REST API, and a responsive HTML/CSS/JavaScript frontend. The application is deployment-ready for platforms such as Render.
